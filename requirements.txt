# LZ-Quant (Latency-Zero) — Complete Dependencies
# All open-source.
#
# IMPORTANT: Install PyTorch with CUDA FIRST (match your CUDA version):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Then install everything else:
#   pip install -r requirements.txt
#
# No GPU? Use CPU-only PyTorch:
#   pip install torch torchvision torchaudio
#   pip install onnxruntime

# ═══════════════════════════════════════════════════════════════════════════════
# MODEL TRAINING
# ═══════════════════════════════════════════════════════════════════════════════
torch>=2.0.0                      # PyTorch (use CUDA version for GPU)
transformers>=4.40.0              # DistilBERT model + tokenizer
datasets>=2.19.0                  # Hugging Face dataset loading
peft>=0.11.0                      # LoRA adapters
accelerate>=0.30.0                # Training utilities
scikit-learn>=1.4.0              # Classification metrics, train_test_split

# ═══════════════════════════════════════════════════════════════════════════════
# ONNX INFERENCE
# ═══════════════════════════════════════════════════════════════════════════════
onnx>=1.16.0                      # ONNX model format
onnxruntime-gpu>=1.18.0          # ONNX inference (GPU) - swap to 'onnxruntime' for CPU-only

# ═══════════════════════════════════════════════════════════════════════════════
# DATA PIPELINE & INGESTION
# ═══════════════════════════════════════════════════════════════════════════════
websockets>=12.0                  # Binance/Alpaca WebSocket connections
aiohttp>=3.9.0                   # Async HTTP client
python-dotenv>=1.0.0             # .env file support for API keys

# ═══════════════════════════════════════════════════════════════════════════════
# DASHBOARD SERVER
# ═══════════════════════════════════════════════════════════════════════════════
fastapi>=0.110.0                  # API framework
uvicorn[standard]>=0.29.0         # ASGI server with WebSocket support

# ═══════════════════════════════════════════════════════════════════════════════
# VISUALIZATION
# ═══════════════════════════════════════════════════════════════════════════════
matplotlib>=3.8.0                # Plotting library
seaborn>=0.12.0                  # Statistical data visualization
numpy>=1.26.0                    # Numerical operations

# ═══════════════════════════════════════════════════════════════════════════════
# CORE UTILITIES (usually included with Python, listed for completeness)
# ═══════════════════════════════════════════════════════════════════════════════
# json - built-in
# csv - built-in
# asyncio - built-in
# datetime - built-in
# collections - built-in
# dataclasses - built-in
# enum - built-in
# typing - built-in
# hashlib - built-in
# re - built-in
# xml.etree - built-in
# urllib - built-in
# signal - built-in
# os - built-in
# sys - built-in
# time - built-in
# math - built-in
# random - built-in
